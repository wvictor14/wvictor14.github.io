[build]
  command = "hugo --gc --minify -b $URL"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.126.1"
  NODE_ENV = "production"

[context.production.environment]
  HUGO_ENV = "production"
