[build]
  command = "hugo mod get -u && hugo --gc --minify -b $URL"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.126.1"
  GO_VERSION = "1.21"
  NODE_ENV = "production"

[context.production.environment]
  HUGO_ENV = "production"
